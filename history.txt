# Change History

## 2026-06-15
- Initialized history.txt to track all changes.
- User Micky confirmed they want changes recorded in history.txt.
- Mapped out the full invite-flow changes needed (Option B).
- Wrote safe migration SQL in schema6.sql: make org_members.user_id nullable, add email/invite_token/expiry columns, fix get_user_org_id() for non-owner staff, add get_user_role() helper, update RLS policies, update org_member_details view with LEFT JOIN.
- Fixed schema6.sql: changed DROP INDEX to ALTER TABLE DROP CONSTRAINT (was failing in Supabase).
- Built the invite system (Option B flow):
  - app/api/team/invite/route.ts — now stores email + invite_token (not inviter's user_id), checks for duplicate invites, supports re-inviting suspended users
  - app/api/team/accept-invite/route.ts — GET validates invite token, POST creates auth user + links to org_members
  - app/auth/accept-invite/page.tsx — accept-invite page showing org name + role, form for name + password only
  - app/api/team/members/route.ts — GET members using org_member_details view
  - src/App.tsx — added AcceptInvitePage import + route registration before session check
- Fixed Express API (api/app.ts) to match the new invite flow:
  - POST /api/team/invite — removed "must sign up first" check, now stores email + token, no user_id required
  - GET /api/team/members — changed to use org_member_details view (fixes relationship cache error)
  - POST /api/team/accept-invite — updated to look up by invite_token instead of user_id
- Fixed Next.js invite route error string to match frontend expectation (limit_exceeded -> seat_limit_reached)
- Added GET handler for /api/team/accept-invite in Express (was missing — caused "Invalid Invitation" error)
- Added APP_URL to .env for correct invite links
- Fixed Express POST /api/team/accept-invite — now creates auth user via admin.createUser() with email_confirm: true (was just updating status without creating the user)
- Updated accept-invite page success screen to show the user's email for login
- Fixed invite API to allow re-sending to already-invited users (was blocking with "already invited" error)
- Express DELETE /api/team/:memberId — smart delete: hard delete for invited members, suspend for active members
- Express PATCH /api/team/:memberId — now supports updating status (for reactivate) in addition to role
- TeamMemberList UI — different actions per status: invited → Delete (hard), active → Remove (suspend), suspended → Reactivate + Delete
- Added confirmation modals for delete and reactivate actions
- Updated Next.js [memberId]/route.ts to match same logic (hard delete for invited, suspend for active, support status updates in PATCH)
- Fixed Express POST /api/team/accept-invite — existing users with `email_exists` error are now looked up and linked to org instead of crashing with 500
- Same fix applied to Next.js accept-invite route
- Updated password + user_metadata on existing accounts via admin.updateUserById
- Fixed RoleContext: non-owner staff no longer default to admin; falls back to operations_coordinator instead
- Added GET /api/team/my-role endpoint in Express (bypasses RLS using admin client) for reliable role fetching
- RoleContext now calls API endpoint first, falls back to direct Supabase query
- Clear localStorage role override on login so fresh sessions fetch real DB role
- FIXED security race condition: default role changed from 'admin' to 'operations_coordinator' in RoleContext to prevent admin UI flash before API resolves
- Migrated all email sending from Resend to Mailjet (invites + dispute letters) across Express API and Next.js routes
- Updated integrations settings UI and API endpoints to use Mailjet API key + secret instead of Resend
- Removed RESEND_API_KEY from .env
- Switched from Mailjet to Brevo (Sendinblue) API — uses direct fetch() to https://api.brevo.com/v3/smtp/email, no SDK dependency
- Updated all email sending in Express (invite, disputes), Next.js (invite, disputes), integrations endpoints, and settings UI to use Brevo
- Professional HTML email template for dispute letters with red gradient header, dispute summary card, formatted letter body, and branding footer

## 2026-06-17
- **Improvement 1 — Flexible Contract Builder:**
  - Added `ChargeItem` type + `RateType` union to `types/index.ts`
  - Rewrote `ContractForm.tsx` with template selector (Road/Air/Ocean/Custom) and dynamic charge items table
  - Rewrote `ContractCard.tsx` to display charge_items
  - Updated AI audit prompt with smart semantic matching for charge items
  - Updated Express (`api/_app.ts`) and Next.js (`app/api/contracts/`) routes to accept/store `charge_items` + `currency`
  - Updated rules-based audit fallback to use `charge_items`
  - SQL migration `schema7.sql` run on Supabase

- **Improvement 2 — PII-safe AI Prompt:**
  - Created `lib/ai/stripPII.ts` with `stripInvoicePII()` and `stripContractPII()`
  - Rewrote `lib/ai/auditInvoice.ts` to use `stripPII` internally with data minimization comment
  - Added "Data & Privacy" tab to settings page with Gemini disclosure notice and what-is/is-not-sent breakdown

- **Improvement 3 — Approve/Dispute Workflow:**
  - Added `approval_reason`, `approval_notes` to `LineItem` type; `resolution_outcome`, `resolution_notes`, `resolved_by` to `Dispute` type; `resolved_by` to `Invoice` type
  - Created `components/invoices/ApprovalReasonModal.tsx` (4 radio options + "Other" text box)
  - Updated `AuditResultPanel.tsx` and `LineItemTable.tsx` — show approval reason modal when discrepancy > 0
  - Updated `app/api/line-items/[id]/route.ts` and `api/_app.ts` PATCH — accept and store `approval_reason`/`approval_notes`
  - Updated `api/_app.ts` POST `/api/disputes/:id/resolve` — accepts `resolution_outcome`, `resolution_notes`, `resolved_by`
  - Updated `app/disputes/[id]/page.tsx` — full resolve form with outcome radios, credit amount, resolution notes
  - Updated `app/invoices/[id]/page.tsx` — status banner shows contextual state based on line item status, disputes, resolution
  - Fixed `tsconfig.json` to exclude `app/` directory (Next.js types not available locally)

- **Upload Type fix (Single vs Batch grouping):**
  - SQL migration: `ALTER TABLE invoices ADD COLUMN upload_type text DEFAULT 'single'`
  - SQL cleanup: cleared `batch_id` from single-invoice batches (runs on Supabase)
  - Express `api/_app.ts` — single upload route now sets `upload_type: 'single'`
  - Next.js `app/api/invoices/upload/route.ts` — single upload route sets `upload_type: 'single'`
  - Next.js `app/api/invoices/batch-upload/route.ts` — batch upload route sets `upload_type: 'batch'`
  - `components/invoices/DataTable.tsx` — rewrote grouping to use `upload_type`: single invoices as plain rows, batch invoices grouped under collapsible headers (default collapsed); batch header shows folder icon, filename, date, count, chevron; Source column shows gray "Single" pill or indigo "Batch" pill
  - `app/invoices/page.tsx` — renamed filter tabs from "All Uploads/Single Files/Batch Packs" to "All/Single/Batch"
  - Fixed `syncParamsFromURL()` — was setting search to batch UUID, now only sets batchFilter

- **Gemini retry + batch resilience:**
  - `lib/ai/geminiClient.ts` — added `callGeminiWithRetry()` with 3 retries + exponential backoff (2s/4s/8s) on 429/503 errors; `generateGeminiContent()` uses it automatically
  - `lib/ai/extractInvoice.ts` — now uses `callGeminiWithRetry()` for retry on 503
  - `app/api/invoices/batch-upload/route.ts` — audit call wrapped in try/catch; on failure, marks invoice as flagged with 0 line items (was stuck in "auditing" forever)
  - `BatchProgressTracker.tsx` — shows amber warning banner after 30s of processing: "AI model is experiencing high demand..."

- **Bug fixes:**
  - Removed generated carrier_email guess (was `billing-claims@...`) from dispute creation — user now enters email manually
  - `app/invoices/[id]/page.tsx` — rewrote `fetchInvoiceData()` to use Supabase directly instead of Express API route (fixes "Unexpected token '<'" JSON parse error when Express route wasn't reachable)
   - `lib/email.ts` — added `tls: { rejectUnauthorized: false }` to nodemailer transport to fix Gmail "self-signed certificate in certificate chain" error

## 2026-06-18
- **Fix: Invoices not appearing after upload:**
  - Express batch upload (`api/_app.ts`): added `upload_type: 'batch'` to insert payload (was missing, causing batch invoices to default to 'single')
  - Upload page (`app/invoices/upload/page.tsx`): dispatches `invoices-updated` event on batch completion so invoices page auto-refreshes
  - Added `GET /api/invoices?user_id=` Express endpoint (bypasses RLS via service role key) as fallback
  - Invoices page (`app/invoices/page.tsx`): falls back to Express API if direct Supabase query returns 0 invoices or errors; shows error banner on query failure
  - DataTable (`components/invoices/DataTable.tsx`): `getUploadType` now checks `batch_id` first — any invoice with a `batch_id` is classified as 'batch' regardless of `upload_type` value

- **Fix: AI audit producing $0 line items (PII stripping regression):**
  - `lib/ai/stripPII.ts`: `stripContractPII()` now converts old-format rate columns into `charge_items` format as fallback when the contract's `charge_items` array is empty. Without this, the AI had no rates to compare against and produced zero expected amounts. Old columns converted: `base_rate_per_lb` → Per lb, `base_rate_per_mile` → Per mile, `fuel_surcharge_pct` → Percentage, plus flat fee columns.
  - Added diagnostic logging to batch upload route to trace extraction output (line items count, total billed, etc.)
  - Batch upload now checks `extracted_data?.line_items` as fallback for line items

- **Fix: "Edit Agreement" opening new contract form instead of editing:**
  - `src/App.tsx`: now passes `contractId` prop to `ContractForm` when editing
  - `components/contracts/ContractForm.tsx`: accepts `contractId` prop; fetches contract from API when `contract` prop is null (fixes case where App.tsx state doesn't have the contract in memory)

- **Fix: Single-file uploads appearing in Batch tab:**
  - `api/_app.ts`: batch upload now sets `upload_type: 'single'` for single-file uploads, `'batch'` for multi-file uploads
  - `DataTable.tsx`: `getUploadType` restored to check `upload_type` first, falls back to `batch_id`

- **Fix: Column alignment in All/Batch tabs:**
  - `DataTable.tsx`: batch header row now uses 8 individual `<td>` elements instead of `colSpan={4}`, ensuring all rows have identical column structure for proper alignment

- **Fix: "Edit Agreement" still showing empty form (GET endpoint missing):**
  - Added `GET /api/contracts/:id` route to Express API (`api/_app.ts`) — was completely missing, causing the fetch in ContractForm to 404 silently
  - `ContractForm.tsx`: added `useEffect([resolvedContract])` to sync form fields (carrierName, effectiveDate, expiryDate, minimumCharge, currency, chargeItems) when API fetch completes
  - `ContractForm.tsx`: `loadingContract` initial state now `!!contractId && !contract` (true when we know we need to fetch), preventing empty form flash before fetch starts

- **Removed Integrations tab from Settings:**
  - `app/settings/page.tsx`: removed integrations tab button, content panel, unused state variables (`loadingIntegrations`, `gmailConfigured`, `anthropicKey`, `isAnthropicConnected`, `showAnthropicKey`), `handleSaveIntegrations` handler, and unused icon imports (`Sliders`, `Sparkles`, `Mail`, `Eye`, `EyeOff`, `CheckCircle`)

- **Created marketing overview document:**
  - Created `FREIGHTAUDIT_AI_OVERVIEW.md` with full project description, problem/solution, workflow guide, features table, target personas, tech stack, and pricing

## 2026-06-24 — Super Admin Dashboard (Full Build)
- **Architecture discovery:** Identified project is Vite + Express SPA (not standard Next.js). Adapted admin dashboard build accordingly — no middleware.ts, no server components, all client-side.
- **Security layer:**
  - Created `src/components/admin/useAdminAuth.ts` — React hook that calls `/api/admin/check` (server-side, uses service role key) to verify super admin status without RLS issues
  - Created `api/_app.ts` `requireSuperAdmin` Express middleware — verifies JWT token + super_admins table membership before any admin API route
  - Added `/api/admin/check` public endpoint (before middleware) for client-side super admin verification
  - Admin pages redirect to `/auth/login` via `window.location.href` if not authenticated as super admin
- **Modified `src/App.tsx`:** Added `AdminApp` import + `/admin` route case (renders before existing route chain)
- **Modified `api/_app.ts`:** Added 12 admin API routes — stats, orgs (list/detail/patch), users (list/patch), revenue, activity, abuse, notes — all behind `requireSuperAdmin` middleware
- **Modified `components/layout/Sidebar.tsx`:** Added dynamic "Admin Panel" link that fetches `/api/admin/check` and only renders for super admins (under "Admin" nav section)
- **Admin pages created** (`src/components/admin/`):
  - `AdminApp.tsx` — Shell with auth guard, sidebar, topbar, and sub-routing for all admin pages
  - `AdminSidebar.tsx` — 200px fixed dark sidebar, nav groups (OVERVIEW/USERS/FINANCIAL/SYSTEM), active state tracking, "Back to App" link
  - `AdminTopBar.tsx` — Live clock, "Super Admin" fuchsia badge, avatar
  - `AdminStatCard.tsx` — Reusable stat card (label, value, trend)
  - `AdminOverview.tsx` — 4 stat cards (orgs, users, invoices, savings) + recent orgs table + recent activity feed
  - `AdminOrganizations.tsx` — Search/filter table with plan/status badges, links to detail
  - `AdminOrgDetail.tsx` — Full org info, members table with suspend/unsuspend, recent invoices, admin notes (read/write), usage bar, danger zone (suspend/change plan)
  - `AdminUsers.tsx` — Searchable user table with role badges, inline suspend/unsuspend + view detail link
  - `AdminUserDetail.tsx` — User profile card, activity timeline, danger zone
  - `AdminRevenue.tsx` — MRR/ARR stat cards, stacked bar chart (12 months), pie chart (plan distribution), per-org breakdown table with utilization progress bars
  - `AdminActivity.tsx` — Filterable audit log feed (date range, action type, search), user emails enriched from auth.users, pagination, CSV export
  - `AdminAbuse.tsx` — 4 detection rules (high uploads, rapid user creation, high dispute volume, inactive paid orgs), persistent dismissals via admin_notes, auto-refresh every 30s, manual refresh button
  - `AdminSettings.tsx` — Plan limits display, current super admin info, CSV export buttons
- **Design system:** Dark theme (#09090B background, #111113 surfaces, #E879F9 fuchsia accent, Geist/Inter font, JetBrains Mono for numbers) — completely separate from main app's indigo/gray design
- **FK join fixes:** Resolved PostgREST foreign key relationship errors across orgs, users, and activity routes by removing join syntax and enriching with separate queries
- **Settings freeze fix:** Removed direct `super_admins` query from browser (RLS hung) — now shows current session user info instead
